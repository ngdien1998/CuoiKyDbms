@model IEnumerable<QuanLyNhaHang.Models.DataModels.ChiTietSlideAnh>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SlideAnh.TenSlide)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UrlHinh)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MoTa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SlideAnh.TenSlide)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UrlHinh)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MoTa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDChiTietSlide }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDChiTietSlide }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDChiTietSlide })
        </td>
    </tr>
}

</table>
